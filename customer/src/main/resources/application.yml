eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/
server:
  port: 8081
spring:
  application:
    name: customer
vehicle:
  application:
    name: vehicle
  service: /vehicle/


---
spring:
  profiles: docker
  
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${REGISTRY_URL}